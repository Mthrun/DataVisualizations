PKG_CPPFLAGS += -DRCPP_PARALLEL_USE_TBB=1
PKG_CXXFLAGS += $(shell "${R_HOME}/bin/Rscript" -e "RcppParallel::RcppParallelCxxFlags()")
PKG_LIBS     += $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS) \
                $(shell "${R_HOME}/bin/Rscript" -e "RcppParallel::RcppParallelLibs()")
